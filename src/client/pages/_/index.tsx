import { Typography } from "@mui/material";
import Layout from "../../components/layout";

const Home: React.FC = () => {
  return (
    <Layout>
      <Typography>Hi</Typography>
    </Layout>
  );
};

export default Home;
